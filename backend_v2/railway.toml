[build]
builder = "DOCKERFILE"

[deploy]
preDeployCommand = "python apply_schema_updates.py"
startCommand = "sh -c 'uvicorn main:app --host 0.0.0.0 --port $PORT'"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
